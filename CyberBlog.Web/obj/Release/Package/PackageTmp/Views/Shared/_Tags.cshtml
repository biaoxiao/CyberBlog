@model IList<CyberBlog.ViewModel.TagViewModel>
<h4 class="side-bar">Tags</h4>
<ul class="list-unstyled list-inline">
	@if (Model.Count> 0)
	{
		foreach (var item in Model)
		{
			<li>
				<a href="/tag/@item.UrlSlug">@item.Tag.ToLower()</a>
			</li>
		}
	}
</ul>

